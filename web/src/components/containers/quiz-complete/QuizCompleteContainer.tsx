import React from 'react';
import QuizComplete from '../../presentations/quiz-complete/index';

const QuizCompleteContainer: React.FC = () => {
  return <QuizComplete />;
};

export default QuizCompleteContainer;
