import styled from 'styled-components';
import DarkButton from './DarkButton';

const BigDarkButton = styled(DarkButton)`
  width: 268px;
  height: 64px;
  font-size: 24px;
`;

export default BigDarkButton;
